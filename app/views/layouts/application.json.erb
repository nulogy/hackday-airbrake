{
  "result": <%= yield %>,
  "pagination": {
    "offset": <%= @pagination[:offset] || '""'.html_safe %>,
    "limit": <%= @pagination[:limit] || '""'.html_safe %>,
    "rowCount": <%= @collection.except(:limit, :offset).count %>
  }
}